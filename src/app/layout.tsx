import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'
import Link from 'next/link'
import { FiPlus } from 'react-icons/fi'
import QueryProvider from './providers/QueryProvider'

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin']
})

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin']
})

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app'
}

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang='en' dir='rtl'>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <QueryProvider>
          <header className='w-full h-20 text-center bg-[#008B8B] '>
            <ul className='flex justify-between p-6'>
             
              <li>
                <Link
                  href='/add'
                  className='flex items-center justify-center -mt-2 w-[50px] h-[50px] bg-white rounded-full hover:bg-[#008B8B] transition-all duration-300 group'
                >
                  <FiPlus
                    size={30}
                    className='text-[#008B8B] group-hover:text-white transition-colors duration-300'
                  />
                </Link>
              </li>
               <li>
                <h1 className='text-[60px] pt-2 font-bold bg-gradient-to-b from-white from-45% via-[#008B8B] via-60%  to-90% bg-clip-text text-transparent'>
                  NOTES
                </h1>
              </li>
            </ul>
          </header>
          {children}
        </QueryProvider>
      </body>
    </html>
  )
}